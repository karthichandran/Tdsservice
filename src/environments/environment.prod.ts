export const environment = {
  production: false,
  baseApiUrl: 'http://localhost:57046/',  // For connecting to server running elsewhere update the base API URL
  serverUrl: '',
  oauth: {
    enabled: false,  // For connecting to Mifos X using OAuth2 Authentication change the value to true
    serverUrl: ''
  }
};
